import { redirect } from 'next/navigation';
export default function Discord() {
  return redirect('https://discord.gg/JSQDUuc6zm'); // This is the eternal discord link that links to the server
  // Unless someone removes the above discord link from the server, we should be fine
}
